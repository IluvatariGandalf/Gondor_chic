import { Routes } from '@angular/router';
import { PageAccueilComponent } from './vue/page-accueil/page-accueil.component';

export const routes: Routes = [
    {
        path : '', 
        component: PageAccueilComponent
    }
];
